language: node_js
node_js:
  - "6"
install:
  - npm install
  - npm install gulp vinyl-ftp minimist gulp-images-resizer fancy-log --save-dev
script:
  - gulp
after_success:
#  gulp deploy --user $FTP_USER --password $FTP_PASSWORD --server $FTP_SERVER
  find thumb -type f -exec curl -v --ssl -u $FPT_USER:$FTP_PASSWORD --ftp-create-dirs -T {} ftp://${FTP_SERVER}/{} \;